export {};

declare global {
  namespace ReactNativePaper {
    interface ThemeColors {
      gray: string;
      malibu: string;
      pictonBlue: string;
      white: string;
    }

    interface Theme {
      myOwnProperty: boolean;
    }
  }
}
